/* global it, describe */
var nop = function () {};

describe('dapple IPFS wrapper', function () {
  it.skip('adds and gets local flat files', nop);
  it.skip('adds and gets a local directory', nop);
  it.skip('gets a remote file from IPFS', nop);
});
